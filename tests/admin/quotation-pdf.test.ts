import assert from "node:assert/strict";
import test from "node:test";
import { customerQuotationPdf } from "../../lib/admin/quotation-pdf";

const saved={quotation_number:"KD-Q-2026-001",quotation_date:"2026-09-10",freight_type:"Sea Freight",final_amount:125000,customer_snapshot:JSON.stringify({name:"Ada Customer",company:"Example Trading",mobile:"09171234567",email:"ada@example.com",address:"Manila"}),cargo_snapshot:JSON.stringify({description:"Household wares",quantity:2,unitType:"boxes",length:40,width:30,height:20,measurementUnit:"cm",cbm:.024,weight:12,origin:"Guangzhou",originWarehouse:"China Warehouse"}),pricing_snapshot:JSON.stringify({densityCharge:999999}),backend_cost:999999,override_reason:"Internal reason",notes:"Admin notes"};
test("customer quotation PDF uses saved customer-facing data only",()=>{const pdf=new TextDecoder().decode(customerQuotationPdf(saved));assert.ok(pdf.startsWith("%PDF-1.4"));assert.match(pdf,/KD-Q-2026-001/);assert.match(pdf,/Ada Customer/);assert.match(pdf,/PHP 1,250.00/);assert.match(pdf,/Malabon Warehouse/);for(const forbidden of ["999999","Internal reason","Admin notes","densityCharge","backend_cost"])assert.doesNotMatch(pdf,new RegExp(forbidden));});
test("customer quotation PDF is a single A4 page",()=>{const pdf=new TextDecoder().decode(customerQuotationPdf(saved));assert.match(pdf,/\/MediaBox \[0 0 595 842\]/);assert.match(pdf,/\/Count 1/);});
